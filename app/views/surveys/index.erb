<h2>Available Surveys</h2>
<ul class="available-surveys">
<% @surveys.each do |survey| %>
  <li><a href="<%= "/surveys/#{survey.id}/responses/new" %>">
    <%= survey.title %></a> <span>(<%= pluralize(survey.questions.size, 'questions') %>)</span>
    </li>
<% end %>
</ul>