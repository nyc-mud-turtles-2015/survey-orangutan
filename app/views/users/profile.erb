<h2>Your Surveys</h2>
<ul class="your-surveys">
<% current_user.authored_surveys.each do |survey| %>
  <li><a href="<%= "/surveys/#{survey.id}" %>">
    <%= survey.title %></a> <span>(<%= pluralize(survey.questions.size, 'questions') %>)</span>
    </li>
<% end %>
</ul>